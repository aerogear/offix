"""
@model
@datasync
@datastore
"""
type Todo {
  id: ID!
  title: String
  description: String
  completed: Boolean
}

"""
@model
@datasync
@datastore
"""
type User {
  id: ID!
  name: String
  # TODO
  # todos: [Todo!]!
}
